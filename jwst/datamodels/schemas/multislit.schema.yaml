allOf:
- $ref: core.schema.yaml
- type: object
  properties:
    slits:
      type: array
      title: An array of slits
      items:
        $ref: slit.schema.yaml
$schema: http://stsci.edu/schemas/fits-schema/fits-schema
