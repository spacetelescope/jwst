Add an option to correct noise in TSO data with reference to a median image (``background_method=median_image``).
Also mask expected spectral traces for NIRISS SOSS data when ``mask_science_regions=True``.
