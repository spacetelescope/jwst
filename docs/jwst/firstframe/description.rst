
Description
===========

The MIRI first frame correction step flags the first group in every integration
as bad (the DO_NOT_USE group data quality flag is added), if the number of
groups is greater than 3. No correction or flagging is done otherwise.
