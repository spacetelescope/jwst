Step Arguments
==============
The ``hlsp`` step has the following optional argument.

``--annuli_width`` (integer, default=2)
  Specify the width, in pixels, of the annuli to use when computing the contrast
  curve data.
