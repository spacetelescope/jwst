Step Arguments
==============
The ``klip`` step has the following optional argument.

``--truncate`` (integer, default=50)
  Specify the number
  of KL transform rows to keep when computing the PSF fit to the target.
